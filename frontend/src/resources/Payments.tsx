//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const PaymentsTitle = () => {
  const record = useRecordContext();
  return <span>Payments {record ? `"${record.paymentMethods}"` : ''}</span>;
};

export const PaymentsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="paymentMethods" />
      <TextField source="defaultPaymentMethod" />
      <TextField source="paymentHistory" />
      <ReferenceField source="driverPayout" reference="Drivers" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const PaymentsEdit = () => (
  <Edit title={<PaymentsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="paymentMethods" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="defaultPaymentMethod" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="paymentHistory" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="driverPayout" reference="Drivers" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const PaymentsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="paymentMethods" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="defaultPaymentMethod" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="paymentHistory" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="driverPayout" reference="Drivers" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  <ReferenceInput
    source="driverPayout"
    label="driverPayout"
    reference="Drivers"
    alwaysOn
  />,
];
